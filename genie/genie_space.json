{
  "title": "Clover Store Ops",
  "description": "Store Operations and Labor assistant for Clover restaurant locations. Answers questions about staffing, foot traffic, labor gap, and scheduling across the fleet.",
  "tables": [
    "clover_spatial_catalog.gold.foot_traffic_daily",
    "clover_spatial_catalog.gold.locations",
    "clover_spatial_catalog.gold.store_ops",
    "clover_spatial_catalog.gold.v_daypart_coverage",
    "clover_spatial_catalog.gold.v_traffic_anomalies"
  ],
  "general_instructions": "This Genie space is a Store Operations and Labor assistant for Clover restaurant locations.\n\nKey definitions:\n- labor_gap = ideal_hours - scheduled_hours (positive means understaffed, negative means overstaffed)\n- staffing_status = 'understaffed' when labor_gap >= 8, 'overstaffed' when labor_gap <= -8, 'balanced' otherwise\n- The target labor productivity rate is 165 visits per labor-hour\n- ideal_hours is derived from forecast_visits divided by 165 (the visits-per-labor-hour target)\n- traffic_delta_pct is the trailing-7-day visit count vs the prior 7-day period, expressed as a percentage change; a negative value indicates a localized foot-traffic drop\n- anomaly_driver describes the category of cause for an unusual traffic pattern (e.g. weather, event, construction)\n\nWhen answering staffing questions, always surface store_id, name, staffing_status, labor_gap, scheduled_hours, and ideal_hours. For traffic questions include traffic_delta_pct and recent_visits. For fleet summaries group by staffing_status.",
  "sample_queries": [
    {
      "title": "Understaffed stores ranked by labor gap",
      "sql": "SELECT store_id, name, format, labor_gap, scheduled_hours, ideal_hours, forecast_visits\nFROM clover_spatial_catalog.gold.store_ops\nWHERE staffing_status = 'understaffed'\nORDER BY labor_gap DESC"
    },
    {
      "title": "Recommended labor hours per store sized to 165 visits per labor-hour",
      "sql": "SELECT store_id, name, forecast_visits,\n  scheduled_hours,\n  ROUND(forecast_visits / 165.0, 1) AS ideal_hours,\n  ROUND(forecast_visits / 165.0 - scheduled_hours, 1) AS labor_gap\nFROM clover_spatial_catalog.gold.store_ops\nORDER BY labor_gap DESC"
    },
    {
      "title": "Stores with the largest week-over-week traffic drops",
      "sql": "SELECT store_id, name, traffic_delta_pct, recent_visits, base_traffic, anomaly_driver\nFROM clover_spatial_catalog.gold.v_traffic_anomalies\nWHERE traffic_delta_pct < 0\nORDER BY traffic_delta_pct ASC\nLIMIT 20"
    },
    {
      "title": "Daypart coverage gaps across the fleet",
      "sql": "SELECT store_id, store_name, daypart, scheduled_coverage, traffic_index,\n  traffic_index - scheduled_coverage AS coverage_gap\nFROM clover_spatial_catalog.gold.v_daypart_coverage\nORDER BY coverage_gap DESC\nLIMIT 30"
    },
    {
      "title": "Single-store drill-down by name",
      "sql": "SELECT s.store_id, s.name, s.format, s.zip, s.sqft,\n  s.recent_visits, s.base_traffic, s.forecast_visits,\n  s.scheduled_hours, s.ideal_hours, s.labor_gap,\n  s.staffing_status, s.traffic_delta_pct, s.anomaly_driver,\n  l.lat, l.lon\nFROM clover_spatial_catalog.gold.store_ops s\nJOIN clover_spatial_catalog.gold.locations l ON s.store_id = l.store_id\nWHERE LOWER(s.name) LIKE LOWER('%:store_name%')"
    },
    {
      "title": "Fleet staffing summary counts by staffing_status",
      "sql": "SELECT staffing_status,\n  COUNT(*) AS store_count,\n  ROUND(AVG(labor_gap), 1) AS avg_labor_gap,\n  SUM(forecast_visits) AS total_forecast_visits,\n  ROUND(SUM(forecast_visits) / NULLIF(SUM(scheduled_hours), 0), 1) AS fleet_visits_per_labor_hour\nFROM clover_spatial_catalog.gold.store_ops\nGROUP BY staffing_status\nORDER BY CASE staffing_status WHEN 'understaffed' THEN 1 WHEN 'balanced' THEN 2 WHEN 'overstaffed' THEN 3 ELSE 4 END"
    }
  ]
}
